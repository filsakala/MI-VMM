<%= form_for(@n_network) do |f| %>
  <% if @n_network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@n_network.errors.count, "error") %> prohibited this n_network from being saved:</h2>

      <ul>
      <% @n_network.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
